---
# Windows Client Controller
# Manages Windows 11 KubeVirt VM lifecycle (install / uninstall / reinstall / status).
# Full documentation with architecture diagrams: windows-client/README.md
#
# Usage:
#   ansible-playbook windows-client-controller.yaml -e action=install
#   ansible-playbook windows-client-controller.yaml -e action=status
#   ansible-playbook windows-client-controller.yaml -e action=uninstall
#   ansible-playbook windows-client-controller.yaml -e action=reinstall
#   ansible-playbook windows-client-controller.yaml -e action=gpu-setup
#   ansible-playbook windows-client-controller.yaml -e action=gpu-setup -e gpu_confirm=yes
#   ansible-playbook windows-client-controller.yaml -e action=gpu-claim
#   ansible-playbook windows-client-controller.yaml -e action=gpu-release
#   ansible-playbook windows-client-controller.yaml -e action=network-install
#   ansible-playbook windows-client-controller.yaml -e action=network-uninstall
#
# Key variables (override with -e):
#   win_bridge_host_ip   Host-side IP on br-vm bridge (default: 192.168.100.1)
#   win_bridge_prefix    Prefix length for br-vm host IP (default: 24)
#   vm_name              VM name in Kubernetes (default: win11client)
#   kubevirt_namespace   Namespace to deploy into (default: default)
#   windows_admin_password  Local Administrator password (default: SecureP@ssw0rd!)
#   iso_path             Path to Windows 11 ISO file (default: ./win11client.iso)
#   iso_download_url     URL to download ISO if not present
#   vm_cpu_cores         Number of vCPU cores (default: 4)
#   vm_memory            VM memory (default: 8Gi)
#   system_disk_size     System disk size (default: 64Gi)
#   installer_iso_size   PV size for Windows ISO (default: 8Gi)
#   virtio_iso_path      Optional: custom path to virtio-win.iso
#   iso_download_url     URL to download ISO if not present
#   enable_gpu           Pass GPU to the VM (requires gpu-setup + reboot first)
#   gpu_pci_slot         PCI address of GPU to pass through (default: 0000:03:00.0)
#   gpu_vendor_id        GPU vendor:device PCI IDs (default: 10de:2204)
#   gpu_audio_id         GPU audio vendor:device PCI IDs (default: 10de:1aef)
#   gpu_confirm          Skip interactive confirmation in gpu-setup (default: no)
#   gpu_action           Internal: set by controller per action (setup/claim/release)
#   nvidia_driver_version  NVIDIA driver version to install (default: 591.86)
#   nvidia_driver_url      Direct download URL for the NVIDIA driver .exe
#   nvidia_reinstall       Set to 'true' to force re-download and reinstall even if already installed (default: false)
#   windows 11 urls      https://www.windowscentral.com/microsoft/windows-11/microsofts-official-windows-11-version-25h2-rtm-iso-media-is-now-available-download-all-28-languages-here-for-x64-or-arm64

- name: Windows Client Controller
  hosts: localhost
  become: yes
  vars:
    kubevirt_operator_namespace: "kubevirt"
    ansible_python_interpreter: /opt/kubevirt-ansible-venv/bin/python
    # NVIDIA Game Ready Driver — update these when a new driver is released
    nvidia_driver_version: "591.86"
    nvidia_driver_url: "https://uk.download.nvidia.com/Windows/591.86/591.86-desktop-win10-win11-64bit-international-dch-whql.exe"

  tasks:
    - name: Set default variables
      ansible.builtin.set_fact:
        action: "{{ action | default('install') }}"
        vm_name: "{{ vmName | default('win11client') }}"
        vm_cpu_cores: "{{ vm_cpu_cores | default(4) | int }}"
        vm_memory: "{{ vm_memory | default('8Gi') }}"
        system_disk_size: "{{ system_disk_size | default('64Gi') }}"
        storage_dir: "{{ storage_dir | default('/var/lib/kubevirt') }}"
        windows_admin_password: "{{ windows_admin_password | default('SecureP@ssw0rd!') }}"
        win_bridge_host_ip: "{{ win_bridge_host_ip | default('192.168.100.1') }}"
        win_bridge_prefix: "{{ win_bridge_prefix | default('24') }}"

    - name: Set kubevirt_namespace default if not provided
      ansible.builtin.set_fact:
        kubevirt_namespace: "{{ kubevirt_namespace | default('default') }}"

    - name: Validate action parameter
      ansible.builtin.fail:
        msg: "Invalid action '{{ action }}'. Must be one of: install, uninstall, reinstall, reinstall-nogpu, install-nogpu, status"
      when: action not in ['install', 'uninstall', 'reinstall', 'reinstall-nogpu', 'install-nogpu', 'status', 'gpu-setup', 'gpu-claim', 'gpu-release', 'nvidia-driver', 'network-install', 'network-uninstall']

    - name: Display controller banner
      ansible.builtin.debug:
        msg:
          - "🪟 Windows Client Controller"
          - "Action:    {{ action | upper }}"
          - "VM Name:   {{ vm_name }}"
          - "Namespace: {{ kubevirt_namespace }}"
          - "CPU:       {{ vm_cpu_cores }} cores"
          - "Memory:    {{ vm_memory }}"
          - "Disk:      {{ system_disk_size }}"

    # Install
    - name: Execute Windows 11 installation
      ansible.builtin.include_tasks: windows-client/windows11-install.yaml
      when: action == 'install'

    # Install (no GPU)
    - name: Execute Windows 11 installation (no GPU)
      ansible.builtin.include_tasks: windows-client/windows11-install-nogpu.yaml
      when: action == 'install-nogpu'

    # Uninstall
    - name: Execute Windows 11 uninstallation
      ansible.builtin.include_tasks: windows-client/windows11-uninstall.yaml
      when: action == 'uninstall'

    # Status
    - name: Execute Windows 11 status check
      ansible.builtin.include_tasks: windows-client/windows11-status.yaml
      when: action == 'status'

    # GPU passthrough setup (detect → dry-run → confirm → apply)
    - name: Execute GPU passthrough host setup
      ansible.builtin.include_tasks: windows-client/windows11-gpu-setup.yaml
      vars:
        gpu_action: setup
      when: action == 'gpu-setup'

    # GPU claim — unbind nvidia, bind vfio-pci, start Windows VM (no reboot)
    - name: Execute GPU claim for Windows VM
      ansible.builtin.include_tasks: windows-client/windows11-gpu-setup.yaml
      vars:
        gpu_action: claim
      when: action == 'gpu-claim'

    # GPU release — stop Windows VM, unbind vfio-pci, bind nvidia (no reboot)
    - name: Execute GPU release back to Fedora
      ansible.builtin.include_tasks: windows-client/windows11-gpu-setup.yaml
      vars:
        gpu_action: release
      when: action == 'gpu-release'

    # Network: create br-vm bridge + win-lan Multus NAD
    - name: Execute Windows network setup
      ansible.builtin.include_tasks: windows-client/windows-network-install.yaml
      when: action == 'network-install'

    # Network: remove win-lan NAD (br-vm kept if shared with RAC)
    - name: Execute Windows network teardown
      ansible.builtin.include_tasks: windows-client/windows-network-uninstall.yaml
      when: action == 'network-uninstall'

    # Post-install automation tasks (runs over WinRM HTTPS)
    - name: Execute Windows 11 post-install automation
      ansible.builtin.include_tasks: windows-client/windows11-automation.yaml
      vars:
        automation_task: "{{ action }}"
      when: action in ['nvidia-driver']

    # Reinstall (uninstall then install)
    - name: Execute Windows 11 reinstallation
      block:
        - name: Uninstall phase
          ansible.builtin.include_tasks: windows-client/windows11-uninstall.yaml
        - name: Pause between uninstall and install
          ansible.builtin.wait_for:
            timeout: 15
        - name: Install phase
          ansible.builtin.include_tasks: windows-client/windows11-install.yaml
      when: action == 'reinstall'

    # Reinstall (no GPU)
    - name: Execute Windows 11 reinstallation (no GPU)
      block:
        - name: Uninstall phase
          ansible.builtin.include_tasks: windows-client/windows11-uninstall.yaml
        - name: Pause between uninstall and install
          ansible.builtin.wait_for:
            timeout: 15
        - name: Install phase (no GPU)
          ansible.builtin.include_tasks: windows-client/windows11-install-nogpu.yaml
      when: action == 'reinstall-nogpu'

    - name: Display completion message
      ansible.builtin.debug:
        msg:
          - "✅ Windows 11 {{ action | upper }} completed!"
          - ""
          - "Commands:"
          - "  Install:   ansible-playbook windows-client-controller.yaml -e action=install"
          - "  Status:    ansible-playbook windows-client-controller.yaml -e action=status"
          - "  Uninstall: ansible-playbook windows-client-controller.yaml -e action=uninstall"
          - "  Reinstall: ansible-playbook windows-client-controller.yaml -e action=reinstall"
          - "  GPU Setup: ansible-playbook windows-client-controller.yaml -e action=gpu-setup"
          - "  GPU Claim: ansible-playbook windows-client-controller.yaml -e action=gpu-claim"
          - "  GPU Rel:   ansible-playbook windows-client-controller.yaml -e action=gpu-release"
          - "  NVIDIA Drv: ansible-playbook windows-client-controller.yaml -e action=nvidia-driver"
          - ""
          - "Custom options:"
          - "  -e vm_name=mywin11          Custom VM name"
          - "  -e windows_admin_password='P@ss!'  Custom admin password"
          - "  -e vhdx_path='/path/to/win11.vhdx'  Use existing VHDX"
          - "  -e vm_cpu_cores=8 -e vm_memory=16Gi  More resources"
          - ""
          - "GPU passthrough:"
          - "  Step 1 (one-time):  -e action=gpu-setup   then: sudo reboot"
          - "  Use Windows + GPU:  -e action=gpu-claim    (nvidia → vfio-pci, starts VM)"
          - "  Use Fedora AI/CUDA: -e action=gpu-release  (stops VM, vfio-pci → nvidia)"
